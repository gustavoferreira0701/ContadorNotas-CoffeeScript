// Generated by CoffeeScript 2.1.0
(function() {
  var ContadorNotas;

  ContadorNotas = (function() {
    var contar, range;

    class ContadorNotas {};

    range = [100, 50, 20, 10, 5, 2, 1];

    constructor(function() {
      return console.log(contar(100));
    });

    contar = function(valor) {
      var i, len, nota, resultado;
      resultado = [];
      for (i = 0, len = range.length; i < len; i++) {
        nota = range[i];
        resultado.push({
          'nota': nota,
          'quantidade': valor / nota
        });
      }
      return resultado;
    };

    return ContadorNotas;

  })();

}).call(this);
